/** Inbound DTOs. */
@keep
package com.track.training.app.atleta.core.inbound.dtos;

@interface keep {

    // keeps this package even when it's empty,
    // allowing wildcard import
    // "com.track.training.app.customer.core.inbound.dtos.*;"

}
